export enum PublicPages {
  Login = "/",
}

export enum ProtectedLayouts {
  Dashboard = "/dashboard",
  Events = "/events",
  Guests = "/guests",
  Invitations = "/invitations",
  CustomForms = "/custom-forms",
  Analytics = "/analytics",
}
